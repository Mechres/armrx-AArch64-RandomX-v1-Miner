# armrx — Status Tracker

> **Post-alpha archive:** For the full retrospective covering all 210 commits (2026-07-13 to 2026-07-30) and 10 performance tracks A–J, see [`RETROSPECTIVE.md`](RETROSPECTIVE.md).
>
> For the still-active strategic master plan, see
> [`docs/plans/20260727/master-plan-20260727.md`](docs/plans/20260727/master-plan-20260727.md).
>
> The complete alpha-phase status tracker is preserved at
> [`docs/archived/alpha-roadmap.md`](docs/archived/alpha-roadmap.md).
>
> New entries go below this line.

## Current Status (2026-08-01)

**Release readiness: all zero/low-risk audit tiers complete or closed on evidence.** T0-1 (Track G default ON), T0-2 (hardware_concurrency fix), T1-1 (D2 benchmark), T1-2 (perf stat harness), T2-3 (isolcpus-aware worker pinning) all ✅ done and device-verified; T2-1 (PRFM hints) ❌ and T2-2 (dual-issue alignment) ❌ closed as measured regressions. Remaining: T3 items (gated).

### In Progress
(none — all W1–W3 diagnostic/measurement backlog items closed on evidence. W3-2 bisection executed (2026-08-01): the `*_M` memory-op scheduler extension reproduces a deterministic JIT/interpreter divergence under the 200/450-pair scheduler stress tests → **CLOSED as a dead end with evidence**. The `ARMRX_MAX_SWAPS` hook + `test_scheduler_bisect --budget=N` stay as proven-useful diagnostic infra. W3-1 (Track C) shipped/zero-impact; W3-3 (no-ABI trampoline) argued-against by the census. The ~2.2× IPC penalty on the main-VM region remains a real lead but its safe fix is confirmed NOT to be the memory-op scheduler. Next items need the JIT-compiler files, which are gated to comments-only until lifted)

### Completed (post-alpha)
- **W1-1 — region instruction census** (2026-08-01) — 118.96M attributed: superscalar 80.5% / main-VM 9.9% (IPC 0.405) / AES+glue 9.5%; static 58.4M superseded (live body 5,224 A64/call); sample-bias dead, under-count primary; call-count residual resolved same day (16,384/hash exact by construction: 8 run() × 2,048-loop × 1 bl; gap = static-count slack) — `docs/experiments/w11-instruction-census.md`
- **T0-1 — Track G (NEON T-table AES) default ON** (2026-08-01) — commit 4888ba1; E2E A/B same day confirms +1.68% H/s, −2.07% cycles, −4.93% instr — `docs/experiments/track-g-e2e-ab.md`
- **T0-2 — hardware_concurrency() footgun** (2026-07-31) — online_cpu_count() reads `/sys/devices/system/cpu/online`
- **T2-3 — isolcpus-aware worker pinning** (2026-08-01) — workers pin to isolated cores on all `detect_core_order()` paths; default worker count capped to isolated count; on-device verified (workers 1-7, main 0)
- **T1-2 — clean perf stat path** (2026-08-01) — `--perf-ready` hook + wrapper, device-verified: first clean steady-state run (IPC 0.740, 119M instr/hash @ fixed 765 MHz) — `docs/experiments/t12-perf-ready-first-run.md`
- **T1-1 — D2 on-device benchmark** (2026-08-01) — `--bench-d2-pipeline` harness; device A/B: interleaved −2.77% (IPC 1.735→1.787), ~0.34% E2E — `docs/experiments/t11-d2-microbenchmark.md`
- **T2-1 — PRFM hints (closed, regression)** (2026-08-01) — inline main-VM `PRFM PLDL1KEEP` per scratchpad LDR: +0.26–0.50% cycles, +0.47% instructions, l1d unchanged. In-order A53 issues the hint in the same window as the dependent LDR — no stall hidden, pure overhead. Corroborates the 2026-07-24 fill-loop removal. Fresh measurement mandated by `jit_compiler_a64_static.S:397` now in hand — `docs/experiments/t21-prfm-hints.md`
- **T2-2 — dual-issue alignment (closed, regression)** (2026-08-01) — NOP-pad long-latency ops (IMUL-family, scratchpad LDRs) to 8-byte group starts in the main-VM JIT: cycles +0.13/+0.20% (worse), instructions +0.47% (NOP overhead), but **IPC +0.3% — the alignment mechanism worked**; the NOP cost just exceeds the dual-issue recovery. Third independent confirmation that added instructions cost real cycles on in-order A53 (after T2-1 and the 2026-07-24 fill-loop hints). Reverted bit-clean; device restored. See `docs/experiments/t22-dual-issue-alignment.md`
- **T3-3 — NEON mul analysis (closed, gate FAIL)** (2026-08-01) — real IMUL_R operand-magnitude gate run via instrumented interpreter sampling (`bench_imul_magnitudes`): **0.003% of 74.1M genuine IMUL_R have one operand ≤ 2^32** (gate ≥30%). Lane-pack premise falsified; F3 follow-on closed on evidence. See `docs/experiments/t33-imul-magnitude-gate.md`
- **W1-4 — XMRig/reference re-baseline (measured, clean-room)** (2026-08-01) — BSD upstream RandomX reference (`scratch_vm_study/upstream_rx`, JIT) run under identical `perf stat` vs `bench_armrx --full-hash-only`, core 3, light 256 MiB. **armrx 132.7M instr/hash IPC 1.369 vs reference 104.8M IPC 1.540 → +26.6% instr, −11% IPC, ~6% faster/nonce (ref ran --verify overhead).** Corrects W1-1: 0.731 was *superscalar sub-region* IPC; overall is 1.369 — NO IPC catastrophe. Gap is modest, instruction-dominated (imm-materialization, W3's 86%); cache-misses at parity. Only untried lever = register-hoist of C* constants (needs gate lift). See `docs/experiments/w14-rebaseline.md`
- **Item 1 — Hardware AESE/AESD AES funnel (2026-08-03):** ✅ **adopted, correctness-verified.** `encrypt_transform`/`decrypt_transform` overridden with the zero-key `vaesmcq_u8(vaeseq_u8(s, zero))` / `vaesimcq_u8(vaesdq_u8(s, zero))` form (byte-identical to T-table; gated on `__ARM_FEATURE_AES`). The 2026-07-20 "AESE incompatible" revert was a *direct* `aese(state,key)` form (AddRoundKey-first = wrong); the zero-key compensation fixes it. **Perf (CORRECTED 2026-08-06 re-baseline):** the original A/B claimed 107.36M → 89.47M (−16.7%, "largest win in history, below XMRig") — the 89.47M was a contaminated-divisor artifact. Reproducible HEAD re-baseline gives **101.10M instr/hash** (two runs identical to 0.00006%), i.e. AES saves **−5.8% (107.36M → 101.10M)**, and armrx is **~7% HEAVIER than XMRig (94.5M)** at 1w. H/s parity still holds (1w 5.11 vs XMRig 5.04, E24 real-pool) — armrx's better IPC (0.667) compensates the heavier instruction count. The correctness evidence (hw==T-table KAT, golden pins, real shares) is solid; only the perf attribution was overstated. See `docs/measurements/2026-08-06-head-rebaseline.md`, `docs/briefs/2026-08-03-hardware-aes-item1.md`.
- **DAG list-scheduler attempt (2026-08-06): CLOSED (negative, not adopted).** `scheduleProgramDag()` behind `ARMRX_DAG_SCHED=1` (default OFF). **Correctness: all 4 on-device gates PASS** (16/16 equivalence, 450+200 stress pairs byte-identical, mining shares) — the multiply-anchored DAG shape avoided the W3-2 trap. **Perf: REGRESSION** — B-M-B-M on core 3, 500-hash window: default **171.63M cyc / 113.77M instr / 5.12 H/s** vs DAG **189.3M cyc / 127.3M instr / 4.38 H/s** = **+10.3% cycles, +11.9% instr, −14.3% H/s** (M1≈M2, reproducible). Adopt gate (≥0.5% cycles improvement in both M runs) → FAIL. Root cause: reorder forces a *longer emitted sequence* (order-dependent codegen), swamping any multiply-latency win. **JIT scheduler reordering is now exhaustively closed** as a lever (also: W3-2 `*_M` scheduler, E20 peephole, T2-1 PRFM, T2-2 dual-issue). Residual ~4.6% gap = instruction *mix/count*, not ordering. **Code kept gated** as a documented negative result. Remaining real lever = worker-cluster placement (fast cluster 0-3), NOT JIT emission. See `changelogs.md` (2026-08-06 latest) + `docs/briefs/2026-08-06-dag-scheduler-attempt.md`.

### Next Up
1. T3 items — design-first, gated (Track C retry blocked on diagnostic)
2. **W4 phase-2 — superscalar C* literal pool: SUPERSEDED by E24 (2026-08-04).** The 2-instr PC-relative LDR pool was correct but *too dense* for the in-order A53 — it put only one load between program-adjacent multiplies and saturated the 4-cycle MAC interlock (`other_interlock_stall` 23.3M/hash, 2.12× XMRig). E24 reverted to the 3-instr `MOVZ`/`MOVN`+`MOVK` form (XMRig-style), padding the multiply gaps → **+7.1% H/s (4.77→5.11, beating XMRig 5.04)** with interlocks at 6.18M/hash (below XMRig). The W4 pool machinery is now dead code (removed). See `changelogs.md` (E24 2026-08-04) and `docs/experiments/perf-tracking.md` §4b.
3. **JIT scheduler reordering is EXHAUSTIVELY CLOSED (2026-08-06):** DAG list-scheduler (latest) joins W3-2 / E20 / T2-1 / T2-2 as closed-negative. Do NOT re-litigate JIT emission-order changes — they only add instructions on the in-order A53. **Next real lever = worker-cluster placement** (Idea 2 from `docs/briefs/2026-08-06-next-levers-brainstorm.md`): bias workers to fast cluster (cores 0-3) to recover the isolcpus-parity 8w win without kernel changes; requires making `bench_armrx`/the harness measure 8w (currently single-threaded). Plus the cheap `-mtune=cortex-a53` default flag (Idea 1).
