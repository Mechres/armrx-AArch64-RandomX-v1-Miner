# Handoff — 2026-08-07 session

**Context:** armrx AArch64/RandomX miner. Last session (2026-08-06) closed the JIT DAG-scheduler
attempt (negative, −14.3% H/s, code kept gated), recorded 3 OPEN bugs, merged `feature/dag-scheduler`
→ `main`. Device: MSM8929 / 8× Cortex-A53 @ 765 MHz, postmarketOS, BusyBox ash, `/tmp`=512M tmpfs
(clears on reboot). Cross-build host: x86_64 + `aarch64-linux-musl-g++` GCC 16.1.0.

**Branch state:** on `main` (was `feature/dag-scheduler`, fast-forward merged). HEAD = `b8e1a6d`
(then this handoff may add `fc1b533` §8.6 + `b8e1a6d` TUI bugs; SIGINT-pool note is UNCOMMITTED
until you commit — read before assuming). Verify with `git log --oneline -3` and `git status`.

---

## OPEN bugs (recorded in docs/STRATEGY.md "Known bugs")
1. **TUI segfault with `ARMRX_DAG_SCHED=1`** — `--tui` under DAG runs ~10-20s (valid H/s) then
   `Segmentation fault`. Hashing correct (16/16 + 450/200 gates pass). Crash in TUI render/shutdown
   under DAG emission order. Only bites if DAG enabled + `--tui`.
2. **TUI garbage artifacts (scheduler-independent)** — `--tui` WITHOUT DAG dumps `armrx` + raw
   control bytes, overlapping lines. Pre-existing TUI rendering bug (escape-seq / multi-thread fd
   write). Non-TUI unaffected.
3. **SIGINT on `--pool` may not exit cleanly (OPEN, needs verification)** — fix `1e5fc52` verified on
   `--mine --mode=light` (host) but a device `--pool` run printed final line but didn't return to
   prompt. Cause unknown: pool-teardown path vs terminal SIGINT delivery. VERIFY via `kill -INT <pid>`
   from a 2nd SSH session (see STRATEGY.md).

## PERF / NEXT-STEP LISTS

### Performance levers remaining (all JIT-emission reordering is EXHAUSTED — do NOT re-litigate)
- **DONE/closed:** W3-2 (`*_M` scheduler), E20 peephole, T2-1 PRFM, T2-2 dual-issue, **DAG
  list-scheduler (2026-08-06, −14.3% H/s, root cause = per-hash planner runtime, NOT emitted-code
  length — see changelogs.md)**. Residual ~4.6% gap = instruction *mix/count*, not ordering.
- **Lever 1 (`-mtune=cortex-a53`): ALREADY DONE + measured NULL — do NOT spend a session.** The cross
  toolchain (`cmake/toolchain-aarch64-musl.cmake:66-67`) already sets `-mtune=cortex-a53`; E18 measured
  it null on-device (4.32 with = 4.32 without). STRATEGY.md:24 lists it closed. (Corrected 2026-08-07:
  earlier handoff draft wrongly listed it as open.)
- **Lever 2 (real 8w win): worker-cluster placement** — bias workers to fast cluster (cores 0-3);
  slow cluster (4-7) loses ~50% under contention. This is what isolcpus buys (armrx 28.4 vs XMRig 27.77
  non-isolated = parity). On MSM8929 both clusters are A53 at one freq, so `detect_core_order()` sets
  `big_core_count_=8` → `BigOnly`==`All` → pins to ALL 8 incl. slow cluster. **Default affinity is
  `AffinityMode::All`** (`mining_engine.hpp:168`), NOT Unpinned (earlier handoff draft was wrong).
  The defensible restatement (2026-08-07 audit): main-thread/worker-0 contention on core 0 in pool mode
  (~1 H/s at 8w ≈ +2-4% potential) is fixable by scheduling only (deprioritize main thread / lighter
  per-second printing); pinning 8 workers to 0-3 would DISCARD the weak cluster's real contribution
  (~16.45 H/s @4w vs ~25.28 @8w) and is a REGRESSION, not a win. Measure via 8w real-pool A/B.
- **Lever 3 (methodology): make `bench_armrx` multi-worker** — `--full-hash-only` is single-threaded
  (`bench_armrx.cpp:798,901`), so the "instruction-mix diff @8w" lever is currently UNMEASURABLE.
  Add `--workers=N` that hashes in parallel + reports aggregate instr/cycle/H-s. Unblocks Lever 2 measurement.
- **Lever 4 (NEW, 2026-08-07 audit): cross-toolchain LTO.** Shipping cross build forces LTO off
  (`toolchain-aarch64-musl.cmake:57`, `ARMRX_DISABLE_LTO=ON`); the GCC-15 musl LTO crash was root-caused
  + fixed (`CMakeLists.txt:379-390`, fortify-headers vs LTO scoped `-U_FORTIFY_SOURCE`), and cross-GCC-16
  LTO was never actually A/B'd. Prior on-device LTO link was +1.9%. Expected ~+1-2%; risk = link crash
  (revert, one session), KATs gate correctness. HYPOTHESIS with positive prior — worth a session.

### Bugs to fix (non-perf)
- TUI render path: single-threaded redraw + mutex around TUI fd writes + correct clear/redraw escape
  (fixes Bug 2; Bug 1 may share root cause or be DAG-order-specific — get a `gdb` backtrace of the
  DAG+`--tui` segfault: one on-device run under `gdb`, alone).
- SIGINT on `--pool`: verify (kill -INT from 2nd session) then fix pool-teardown if needed.

### Device-safety rules (MUST bake into every external-agent prompt / brief — hard-won)
- NEVER run full on-device suite at once; qemu-aarch64 FORBIDDEN for verification.
- Run ONE test/session, fastest-first (`test_jit_equivalence` 16/16 ALONE first = W3-2 gate), cooldown,
  stress suite LAST+ALONE. (docs/TESTING.md §8.1-§8.4)
- scp and ssh SEPARATE; one `scp` per binary, no `ssh ... && scp ...`. (§8.2)
- SSH timeout ≠ remote kill; assume still running, `pkill -9 -f <bin>` as its own bare ssh. (§8.3)
- Fast-cluster pin: correctness tests may pin 0-3 (core-invariant); perf A/B pins BOTH sides equally. (§8.5)
- Stale SSH session mimics device lag → close all SSH + reconnect, don't reboot. (§8.6)

---

## TODO for new session (in priority order)
1. **Commit the SIGINT-pool OPEN note** (already written to STRATEGY.md, uncommitted) + this handoff.
2. **Verify SIGINT-on-pool** (kill -INT from 2nd session) — determine code-bug vs terminal-delivery.
3. **DAG 20-min 8w settle run** (user wants final number): `ARMRX_DAG_SCHED=1 taskset -c 0-3
   /tmp/cross-dag/armrx --pool=... ` for ≥20 min, read settled H/s. Expect ~18 H/s (1w −14% scales;
   consistent with 25→18 observed). This is informational only (DAG already CLOSED).
4. **Lever 1 (`-mtune=cortex-a53`)** — implement + B-M-B-M. First real perf attempt.
5. **Lever 2 (cluster placement)** — after Lever 1, the actual 8w parity play.
6. **TUI bugs** — fix Bug 2 (artifacts) at least; Bug 1 backtrace via gdb.

## 2nd device note
User is adding a **faster A53 variant** as a 2nd test target (Termux/Unisoc? — confirm). It is for
**build/test execution only, NOT metrics** (different silicon → numbers not comparable to MSM8929).
Do not use it for any perf A/B or rate claim. Use MSM8929 for all measurement.

## User preferences / constraints (stable)
- Budget-sensitive (student). Don't waste device time. One test/session discipline is now MANDATORY.
- User runs external planners; Hermes = brief/prompt author + skeptical gate. Don't run planners yourself.
- Don't commit unless asked (except doc/bugfix discipline per AGENTS.md — user has approved doc commits).
- Verify on-device with real gates; qemu is not evidence.

## Key numbers (reference)
- 1w: armrx 5.12 H/s / 113.77M instr / IPC 0.663 (gated 500-hash, core 3). XMRig 5.04 H/s / 94.5M.
- 8w: armrx 26.65 vs XMRig 28.0 = 95.2% (real pool, non-isolated). isolcpus → 28.4 = parity.
- DAG scheduler: 4.38 H/s / 127.3M instr (1w) = −14.3% H/s vs 5.12. CLOSED.
- Clock fixed 765 MHz, no cpufreq (no OPP table). Two 4-core clusters; 0-3 fast, 4-7 slow (~50% under contention).
